# Copie este arquivo para .env (na mesma pasta) e preencha com as SUAS chaves.
# O .env está no .gitignore — nunca será commitado.
GEMINI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
HF_TOKEN=

# Opcional: restringir CORS (em produção use o domínio do seu GitHub Pages)
# ALLOWED_ORIGINS=https://SEUUSUARIO.github.io
